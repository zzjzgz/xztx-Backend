
spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    买了线上数据库后，可以改成线上的数据库
    url: jdbc:mysql://localhost:3306/xztx
    username: root
    password: zzj0806
#    失效時間
  session:
    timeout: 86400
